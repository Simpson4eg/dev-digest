import type { CSSProperties } from "react";

export const s = {
  descriptionBox: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    background: "var(--bg-elevated)",
    padding: 18,
    fontSize: 14,
    color: "var(--text-secondary)",
    whiteSpace: "pre-wrap",
    lineHeight: 1.55,
  } satisfies CSSProperties,

  // ---- Intent Layer panel ----
  intentHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 8,
  } satisfies CSSProperties,
  recomputeBtn: {
    fontSize: 12,
    color: "var(--text-muted)",
    background: "none",
    border: "1px solid var(--border)",
    borderRadius: 5,
    padding: "2px 10px",
    cursor: "pointer",
    lineHeight: 1.6,
  } satisfies CSSProperties,
  intentBox: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    background: "var(--bg-elevated)",
    padding: 18,
    marginBottom: 24,
  } satisfies CSSProperties,
  intentEmpty: {
    border: "1px dashed var(--border)",
    borderRadius: 8,
    padding: 18,
    marginBottom: 24,
    fontSize: 13,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  intentText: {
    margin: "0 0 16px",
    fontSize: 14,
    fontStyle: "italic",
    color: "var(--text-primary)",
    lineHeight: 1.55,
  } satisfies CSSProperties,
  scopeGrid: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 20,
  } satisfies CSSProperties,
  scopeCol: {} satisfies CSSProperties,
  scopeTitle: {
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.07em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
    marginBottom: 8,
  } satisfies CSSProperties,
  scopeEmpty: {
    fontSize: 13,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  scopeUl: {
    listStyle: "none",
    margin: 0,
    padding: 0,
    display: "flex",
    flexDirection: "column",
    gap: 6,
  } satisfies CSSProperties,
  scopeLi: {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    fontSize: 13,
    color: "var(--text-secondary)",
    lineHeight: 1.45,
  } satisfies CSSProperties,

  // ---- Blast Radius panel ----
  blastBox: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    background: "var(--bg-elevated)",
    padding: 18,
    marginBottom: 24,
  } satisfies CSSProperties,
  blastEmpty: {
    border: "1px dashed var(--border)",
    borderRadius: 8,
    padding: 18,
    marginBottom: 24,
    fontSize: 13,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  /** Count chips row under the header (symbols · callers · endpoints · crons). */
  blastCounts: {
    display: "flex",
    flexWrap: "wrap",
    gap: 14,
    marginBottom: 14,
    fontSize: 12,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  blastCount: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
  } satisfies CSSProperties,
  /** A degraded/partial-index badge (honest state, not a blank panel). */
  blastBadge: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    fontSize: 12,
    color: "var(--text-muted)",
    border: "1px solid var(--border)",
    borderRadius: 6,
    padding: "4px 10px",
    marginBottom: 14,
  } satisfies CSSProperties,
  /** One changed-symbol row (clickable to expand its callers). */
  blastSymRow: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    width: "100%",
    background: "none",
    border: "none",
    padding: "8px 0",
    cursor: "pointer",
    textAlign: "left",
    color: "var(--text-primary)",
    borderTop: "1px solid var(--border)",
  } satisfies CSSProperties,
  blastSymName: {
    fontFamily: "var(--font-mono, monospace)",
    fontSize: 13,
    fontWeight: 600,
  } satisfies CSSProperties,
  blastSymKind: {
    fontSize: 11,
    color: "var(--text-muted)",
    textTransform: "uppercase",
    letterSpacing: "0.05em",
  } satisfies CSSProperties,
  blastSymMeta: {
    marginLeft: "auto",
    fontSize: 12,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  /** The expanded caller tree under a symbol. */
  blastCallers: {
    display: "flex",
    flexDirection: "column",
    gap: 4,
    padding: "2px 0 12px 22px",
  } satisfies CSSProperties,
  blastChips: {
    display: "flex",
    flexWrap: "wrap",
    gap: 6,
    padding: "0 0 12px 22px",
  } satisfies CSSProperties,
  blastChip: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    fontSize: 12,
    fontFamily: "var(--font-mono, monospace)",
    color: "var(--text-secondary)",
    background: "var(--bg-subtle, rgba(127,127,127,0.08))",
    border: "1px solid var(--border)",
    borderRadius: 5,
    padding: "2px 8px",
  } satisfies CSSProperties,

  // ---- Why + Risk Brief panel ----
  briefBox: {
    border: "1px solid var(--border)",
    borderRadius: 8,
    background: "var(--bg-elevated)",
    padding: 18,
    marginBottom: 24,
  } satisfies CSSProperties,
  briefEmpty: {
    border: "1px dashed var(--border)",
    borderRadius: 8,
    padding: 18,
    marginBottom: 24,
    fontSize: 13,
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  /** Header row with risk-level badge + regenerate button. */
  briefHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 10,
  } satisfies CSSProperties,
  /** Outdated warning badge — shown when brief was built at an older head sha. */
  briefOutdatedBadge: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    fontSize: 12,
    color: "var(--warning, #d29922)",
    border: "1px solid var(--warning, #d29922)",
    borderRadius: 6,
    padding: "3px 10px",
    marginBottom: 10,
  } satisfies CSSProperties,
  /** risk_level badge — colored by severity, includes a text label (a11y). */
  briefRiskBadge: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    fontSize: 12,
    fontWeight: 700,
    borderRadius: 6,
    padding: "3px 10px",
  } satisfies CSSProperties,
  briefWhat: {
    margin: "0 0 6px",
    fontSize: 14,
    fontWeight: 600,
    color: "var(--text-primary)",
    lineHeight: 1.45,
  } satisfies CSSProperties,
  briefWhy: {
    margin: "0 0 16px",
    fontSize: 13,
    color: "var(--text-secondary)",
    lineHeight: 1.55,
    fontStyle: "italic",
  } satisfies CSSProperties,
  /** Sub-section heading (Risks / Review Focus). */
  briefSection: {
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.07em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
    marginBottom: 8,
    marginTop: 16,
  } satisfies CSSProperties,
  /** One risk card. */
  briefRiskCard: {
    border: "1px solid var(--border)",
    borderRadius: 6,
    padding: "10px 12px",
    marginBottom: 8,
    background: "var(--bg-subtle, rgba(127,127,127,0.04))",
  } satisfies CSSProperties,
  briefRiskTitle: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 13,
    fontWeight: 600,
    color: "var(--text-primary)",
    marginBottom: 4,
  } satisfies CSSProperties,
  briefRiskExplanation: {
    fontSize: 13,
    color: "var(--text-secondary)",
    lineHeight: 1.5,
    margin: "0 0 6px",
  } satisfies CSSProperties,
  briefRiskFiles: {
    display: "flex",
    flexWrap: "wrap",
    gap: 6,
    marginTop: 4,
  } satisfies CSSProperties,
  briefRiskFile: {
    display: "inline-flex",
    alignItems: "center",
    fontSize: 12,
    fontFamily: "var(--font-mono, monospace)",
    color: "var(--text-muted)",
    background: "var(--bg-subtle, rgba(127,127,127,0.08))",
    border: "1px solid var(--border)",
    borderRadius: 4,
    padding: "1px 7px",
  } satisfies CSSProperties,
  /** One review-focus row (link to file:line). */
  briefFocusRow: {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "5px 0",
    borderTop: "1px solid var(--border)",
  } satisfies CSSProperties,
  briefFocusMeta: {
    fontSize: 12,
    color: "var(--text-muted)",
    lineHeight: 1.4,
  } satisfies CSSProperties,
  briefRegenBtn: {
    fontSize: 12,
    color: "var(--text-muted)",
    background: "none",
    border: "1px solid var(--border)",
    borderRadius: 5,
    padding: "2px 10px",
    cursor: "pointer",
    lineHeight: 1.6,
  } satisfies CSSProperties,
} as const;
